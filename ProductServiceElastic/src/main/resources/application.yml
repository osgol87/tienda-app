server:
  port: ${PRODUCTSERVICE_PORT:8081}

spring:
  application:
    name: productservice

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
  instance:
    instance-id: ${spring.application.name}:${random.value}
    prefer-ip-address: true
    hostname: productseervice

elasticsearch:
  host: ${ELASTICSEARCH_HOST:ogonzalezl-6089145741.us-east-1.bonsaisearch.net:443}
  user: ${ELASTICSEARCH_USER:orhmb20dea}
  password: ${ELASTICSEARCH_PASS:91418fl69v}
loggin:
  level:
    tracer: TRACE